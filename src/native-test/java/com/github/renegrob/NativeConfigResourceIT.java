package com.github.renegrob;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeConfigResourceIT extends ConfigResourceTest {

    // Execute the same tests but in native mode.
}
