package com.github.renegrob;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeCacheResourceIT extends CacheResourceTest {

    // Execute the same tests but in native mode.
}